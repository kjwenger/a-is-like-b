val numbers = Array(1, 5, 3, 12, 2)
val sorted = numbers.sorted
val sortedDesc = numbers.sortWith(_ &gt; _)

val words = List("banana", "apple", "cherry")
val sortedWords = words.sorted
val sortedWordsDesc = words.sortWith(_ &gt; _)
