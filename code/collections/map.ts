const numbers: number[] = [20, 19, 7, 12];
const tripled: number[] = numbers.map((x: number) =&gt; x * 3);
const doubled: number[] = numbers.map((x: number) =&gt; x * 2);
console.log(tripled);
console.log(doubled);
