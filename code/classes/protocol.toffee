class Circle
  draw: -&gt; console.log "Drawing a circle"

# ToffeeScript uses duck typing
drawShape = (shape) -&gt;
  if typeof shape.draw is 'function'
    shape.draw()

circle = new Circle()
drawShape circle