#include <iostream>

// Inclusive range operator in C++
// C++ doesn't have a range operator
// Use loops instead
int main() {
    for (int i = 0; i <= 5; i++) {
        std::cout << i << std::endl;
    }
    return 0;
}